# 新建任务功能 PRD

## 1. 背景与目标

新建任务是用户进入系统后发起 AI 工作流的核心入口，承载通用对话、规划执行、专家协作、快捷任务、技能调用、文件处理和多轮任务排队等能力。

本功能目标是提供一个简洁、精致、低干扰的新任务创建体验，让用户可以快速选择合适的工作模式，并在对话过程中持续追加、排队、调整任务。

## 2. 用户角色

- 普通用户：通过自然语言发起问答、写作、PPT、文档处理等任务。
- 专家使用者：选择指定专家完成专业任务。
- 多任务用户：在一个对话过程中连续提交多个任务，并管理排队顺序。
- 项目协作者：在项目上下文中发起任务，并调用项目内专家、技能或资料。

## 3. 功能范围

### 3.1 新建任务首页

- 页面默认展示欢迎语。
- 页面默认展示三个快捷入口：
  - 知识问答
  - 深度写作
  - PPT 创作
- 快捷入口使用轻量扁平胶囊/卡片样式，视觉上不喧宾夺主。
- 点击快捷入口后进入对应任务模式，并将所选类型展示在对话框底部模式选择位置。

### 3.2 对话框

- 对话框作为页面核心输入区域。
- 未开始对话时，对话框位于页面中部偏下。
- 开始对话后，对话框固定在页面底部。
- 对话内容从页面上方开始向下输出。
- 对话内容较多时，页面支持垂直滚动。
- 对话框宽度在未对话和对话中保持一致，不因状态切换变窄。

### 3.3 模式选择

对话框底部提供模式选择控件，位于模型选择之前。

模式包括：

- Agent
  - 定义：全自动执行者，目标驱动，全自动闭环。
  - 适合用户直接提出目标，由系统自动拆解并执行。
- Plan
  - 定义：可控的规划师，计划-审批-分步执行。
  - 适合需要先生成计划、用户确认后再执行的任务。
- 召唤专家
  - 用户可选择某个专家进入专家任务模式。

交互规则：

- 点击模式控件后，下拉菜单出现在按钮上方。
- Agent 和 Plan 之间切换不创建新任务，当前对话保持不变。
- 在对话过程中，从 Agent 或 Plan 切换为专家时，需要重新创建新的任务。
- 已处于专家模式时，选择专家保持原有专家切换逻辑。
- 选择知识问答、深度写作、PPT 创作后，其名称展示在模式选择位置。
- 切换回 Agent 或 Plan 时，清除快捷任务类型。

### 3.4 模式提示

模式下拉菜单中 Agent 和 Plan 旁提供 `i` 提示按钮。

- 鼠标移入 Agent 提示：
  - 全自动执行者，目标驱动，全自动闭环
- 鼠标移入 Plan 提示：
  - 可控的规划师，计划-审批-分步执行
- 提示浮层使用轻量白色浮层，避免遮挡主操作。

### 3.5 召唤专家

点击“召唤专家”后，在模式菜单右侧展示专家弹窗。

弹窗内容：

- 顶部搜索框，placeholder 为“搜索专家...”
- 专家列表
- 底部入口：“在 专家 中查找更多”

搜索规则：

- 根据专家名称进行搜索。
- 默认展示搜索结果前 5 个。
- 列表支持滚动加载更多，每次追加展示更多专家。
- 无结果时展示空状态。

专家选择规则：

- 点击专家后，专家名称展示在对话框模式位置。
- 如果当前已有 Agent 或 Plan 对话，选择专家会重新创建新的任务。
- 专家任务默认进入 Agent 执行模式。

查找更多规则：

- 点击“在 专家 中查找更多”跳转到专家模块。
- 跳转时带入当前搜索词。
- 专家模块展示对应搜索结果。

### 3.6 专家模块跳转新建任务

- 在专家模块点击某个专家后，跳转到新建任务模块。
- 被点击专家会带入新建任务对话框。
- 新任务会清空原有对话、队列和快捷模式状态。
- 输入框可预填专家默认提示词。

### 3.7 模型选择

- 模型选择位于模式选择后方。
- 支持切换当前任务使用的模型。
- 选择专家后，模型选择隐藏且不可操作。
- 模型选择不触发新建任务。

### 3.8 技能选择

- 技能选择位于模型选择后方。
- 在 Agent / Plan 普通模式下展示。
- 选择知识问答、深度写作、PPT 创作后，技能选择隐藏。
- 选择技能后，技能以 `/技能名` 的形式插入到输入框当前光标位置。
- 技能可以插入在文本开头、中间或末尾。
- 支持通过输入 `/` 触发技能联想菜单。
- 最多支持添加 10 个技能。
- 发送消息时，系统从输入文本中识别 `/技能名` 并写入消息元信息。

### 3.9 附件上传

- 默认展示附件上传按钮。
- 选择“知识问答”后，附件上传按钮隐藏。
- 选择深度写作、PPT 创作或 Agent / Plan 普通模式时，附件上传按钮恢复展示。

### 3.10 快捷任务

快捷任务包括：

- 知识问答
  - 输入提示：请输入你要查询的问题或知识点。
  - 选择后隐藏技能选择。
  - 选择后隐藏附件上传。
- 深度写作
  - 输入提示：请输入写作主题、受众、风格和输出要求。
  - 选择后隐藏技能选择。
- PPT 创作
  - 输入提示：请输入演示主题、场景、页数和重点内容。
  - 选择后隐藏技能选择。
  - 支持展示 PPT 模板选择。

### 3.11 PPT 模板

- 当选择 PPT 创作时展示模板区域。
- 模板可包括：
  - 工作汇报
  - 项目方案
  - 产品介绍
  - 培训课件
  - 客户提案
  - 品牌宣传
- 点击模板后，输入框填入对应模板提示词。
- 模板选择会写入当前任务元信息。

### 3.12 对话发送

发送规则：

- 输入为空时，不允许发送。
- 发送后清空输入框。
- 第一、第二次发送，直接进入正常“提问 → 回答”流程。
- 从第三次发送开始，消息先进入消息队列。

正常对话流程：

- 用户消息展示在对话区右侧或用户样式气泡中。
- AI 回答展示在对话区左侧或助手样式气泡中。
- 回答可带有模式、模型、技能、专家等元信息，但需要按当前任务类型展示。
- Agent / Plan 普通任务：
  - 可展示模式、模型、技能等元信息。
  - 未选择技能时不展示技能信息。
- 快捷任务：
  - 可展示快捷任务类型、模型、PPT 模板等元信息。
  - 知识问答、深度写作、PPT 创作选择后，技能不可选，因此不展示技能信息。
- 专家任务：
  - 仅展示专家相关信息。
  - 不展示模式、模型、技能信息。
  - 原因：选择专家后，模型选择和技能选择均不可操作，用户无法在该任务中修改这些元素。

### 3.13 消息队列

队列展示位置：

- 位于对话框上方。
- 紧贴对话框，但不与对话框重叠。

进入队列规则：

- 从第三次对话开始，新发送消息进入消息队列。
- 队列消息每隔 10 秒自动从队列移除。
- 移除后进入正常“提问 → 回答”流程。

队列操作：

- 编辑：支持编辑当前排队消息。
- 保存：保存编辑后的消息。
- 取消：退出编辑状态。
- 上移：将消息发送顺序提前。
- 下移：将消息发送顺序后移。
- 删除：从队列移除消息。
- 引导：立即将该消息移出队列并进入正常对话流程。

队列顺序：

- 系统按队列从上到下顺序处理消息。
- 用户上移/下移后，自动处理顺序以最新队列顺序为准。

### 3.14 上下文状态

- 对话中展示上下文使用进度。
- 支持查看剩余可用上下文、本轮新增上下文、压缩状态。
- 输入内容、技能、专家、快捷模式、PPT 模板会影响上下文估算。

### 3.15 页面状态

未开始对话：

- 展示欢迎语。
- 展示快捷任务入口。
- 对话框位于主视觉区域。

已开始对话：

- 隐藏欢迎语和快捷入口。
- 对话框固定在底部。
- 对话内容和队列支持滚动查看。

重置任务：

- 清空对话消息。
- 清空队列。
- 清空技能状态。
- 清空 PPT 模板状态。
- 重置发送次数。
- 关闭进行中的队列定时器。

## 4. 关键交互流程

### 4.1 普通 Agent 对话

1. 用户进入新建任务。
2. 默认模式为 Agent。
3. 用户输入任务目标。
4. 点击发送。
5. 系统展示用户提问。
6. 系统输出回答。

### 4.2 Plan 模式对话

1. 用户点击模式控件。
2. 用户选择 Plan。
3. 当前任务不重建。
4. 用户输入目标并发送。
5. 系统先输出计划类回答。
6. 后续根据审批/确认继续分步执行。

### 4.3 对话中切换专家

1. 用户已有 Agent 或 Plan 对话。
2. 用户打开模式菜单。
3. 点击召唤专家。
4. 在右侧专家弹窗中选择专家。
5. 系统重新创建新任务。
6. 新建任务中展示所选专家。
7. 原对话和队列清空。

### 4.4 使用技能

1. 用户在输入框中输入问题。
2. 用户点击技能选择。
3. 系统将 `/技能名` 插入当前光标位置。
4. 用户继续编辑问题。
5. 发送后，系统识别文本中的技能并写入消息元信息。

### 4.5 多任务排队

1. 用户完成前两轮正常对话。
2. 第三次发送消息。
3. 消息进入队列。
4. 用户可编辑、上移、下移或删除队列消息。
5. 10 秒后，队首消息进入正常对话流程。

## 5. 视觉与体验要求

- 整体遵循 Apple 风格：精致、简洁、低噪音。
- 使用白色、浅灰、轻阴影和圆角表达层级。
- 字体层级清晰，避免过大的菜单字号。
- 菜单和弹窗保持紧凑。
- 对话框、队列、菜单之间不能重叠。
- 状态变化需要平滑但不过度动画。
- 工具按钮优先使用图标，不使用冗长文字。

## 6. 边界情况

- 输入为空：发送按钮不触发任务。
- 技能超过 10 个：提示最多可添加 10 个技能。
- 专家搜索无结果：展示空状态。
- 队列为空：隐藏队列区域。
- 切换 Agent / Plan：不清空当前对话。
- 对话中切换专家：清空当前对话并新建任务。
- 点击专家模块专家：始终新建专家任务。
- 队列处理中删除队首：重新按最新队列顺序计时处理。

## 7. 数据状态

核心状态包括：

- 当前模式：Agent / Plan / 快捷任务 / 专家
- 当前专家
- 当前快捷任务类型
- 当前模型
- 当前技能
- 当前 PPT 模板
- 对话轮次
- 消息队列
- 队列定时器
- 上下文使用量

## 8. 验收标准

- 用户可以在新建任务页完成 Agent、Plan、专家、快捷任务切换。
- Agent 和 Plan 切换不重建任务。
- 对话中选择专家会重新创建任务。
- 专家模块点击专家后，新建任务页正确展示该专家。
- 技能选择会插入输入框文本中，且支持插入到文本中间。
- 知识问答模式隐藏附件按钮。
- 快捷任务模式隐藏技能选择。
- 第三次及之后发送的消息进入队列。
- 队列支持编辑、删除、上移、下移和立即引导。
- 队列消息 10 秒后自动进入正常对话流程。
- 对话内容较多时页面可滚动。
- 模式菜单在按钮上方展示。
- 召唤专家弹窗在菜单右侧展示并支持搜索。

## 9. 后续可扩展能力

- 支持保存用户常用模式组合。
- 支持自定义队列延迟时间。
- 支持专家推荐排序。
- 支持任务模板收藏。
- 支持项目上下文自动注入。
- 支持真实文件上传和解析。
- 支持 Plan 模式审批节点。
- 支持队列任务批量操作。
