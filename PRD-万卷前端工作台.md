# 万卷前端工作台 PRD

## 1. 需求背景

万卷前端工作台是面向普通用户、业务专家使用者和技能使用者的统一任务执行入口。当前原型以“新建任务”为核心，已覆盖通用智能体、定制智能体、专家调用、技能调用、自动化、文件库、历史入口和个人设置等能力，需要将这些能力收敛为一致、简洁、可持续扩展的工作台体验。

本期建设重点是将“新建任务”打造为统一任务执行入口，把通用智能体 Agent、定制智能体（知识问答、深度写作、PPT 创作）、专家和技能能力统一到同一个任务发起与执行框架中。用户可以通过自然语言快速提出目标，并按需调用模型、专家、已安装技能、附件和自动化能力，形成从“提出目标”到“执行交付、文件沉淀、历史追溯”的完整闭环。

项目模块在原型中已出现，但本期不纳入 PRD 范围，作为二期能力另行规划。

## 2. 产品目标

- 建设统一任务执行入口：以新建任务统一承载通用智能体、定制智能体、专家和技能能力调用。
- 强化通用智能体 Agent 能力：默认支持目标理解、任务拆解、自动执行和多轮闭环。
- 支持定制智能体任务：提供知识问答、深度写作、PPT 创作等高频场景入口。
- 支持可控执行：通过 Plan 模式提供计划生成、确认和分步执行体验。
- 支持专业能力调用：用户可从新建任务或专家模块选择专家，完成专业场景处理。
- 支持技能生态：技能区分技能市场和已安装，支持安装、上传、创建和调用。
- 支持任务资产沉淀：文件库统一管理任务生成文件，支持搜索、预览、定位和下载。
- 支持持续运行：自动化模块支持周期性任务配置、执行状态和历史记录。
- 保持 Apple 风格体验：界面精致、简洁、低噪音，强化字号层级、间距和清晰反馈。

## 3. 本期范围

### 3.1 本期包含

- 新建任务
- 专家模块
- 技能市场与已安装技能
- 添加技能：上传技能、创建技能
- 自动化
- 文件库
- 历史与最近任务
- 个人设置入口

### 3.2 本期不包含

- 项目模块
- 项目列表、项目详情、项目任务和成员协同
- 项目邀请链接、申请加入、同意/拒绝申请
- 项目级指令、项目文件、项目技能、项目专家配置
- 项目上下文自动注入

以上项目相关能力作为二期开发范围，本文档仅在导航中保留“项目（二期）”的占位说明，不展开需求。

### 3.3 功能清单

| 模块 | 功能项 | 本期说明 |
| --- | --- | --- |
| 新建任务 | 通用智能体 Agent | 支持目标理解、任务拆解、自动执行和多轮闭环。 |
| 新建任务 | Plan 模式 | 支持计划生成、确认和分步执行。 |
| 新建任务 | Ask 模式 | 支持轻量问答。 |
| 新建任务 | 定制智能体 | 支持知识问答、深度写作、PPT 创作，并可按场景启用联网搜索和知识库搜索。 |
| 新建任务 | 专家调用 | 支持召唤专家；对话中切换专家需新建任务。 |
| 新建任务 | 技能调用 | 支持调用已安装技能，并以 `/技能名` 插入输入框。 |
| 新建任务 | 模型选择 | 支持模型切换；对话中切换模型需提示性能影响。 |
| 新建任务 | 附件上传 | 支持普通任务附件上传；知识问答场景隐藏附件上传。 |
| 新建任务 | 消息队列 | 上一轮会话未结束时，新发送消息进入队列，支持编辑、删除、上移、下移和自动处理。 |
| 新建任务 | 上下文能力 | 支持上下文使用比例、已使用量/总窗口、剩余可用展示。 |
| 新建任务 | 会话消耗展示 | 在回答下方展示本次回答的模型、耗时、Token 消耗，帮助用户理解执行成本。 |
| 专家 | 专家列表 | 支持专家展示、搜索、分类筛选和点击进入新建任务。 |
| 专家 | 专家团 | 支持专家团展示，用于多智能体协同场景。 |
| 技能 | 技能市场 | 支持技能浏览、搜索、分类和安装。 |
| 技能 | 已安装技能 | 展示当前用户可调用技能，并作为新建任务技能来源。 |
| 技能 | 添加技能 | 支持上传技能和创建技能。 |
| 自动化 | 自动化列表 | 展示任务名称、频率、生效区间、运行状态。 |
| 自动化 | 创建自动化 | 支持任务名称、提示词、频率、生效时间配置。 |
| 自动化 | 执行历史 | 展示执行摘要、执行时间和状态。 |
| 文件库 | 文件分组 | 按有文件输出的任务会话分组展示。 |
| 文件库 | 文件搜索 | 支持搜索任务名称或文件名称。 |
| 文件库 | 文件操作 | 支持预览文件、定位到任务、下载文件。 |
| 文件库 | 滚动加载 | 文件列表独立滚动，后续接口按 cursor 加载。 |
| 历史 | 最近任务 | 左侧展示近期任务入口。 |
| 历史 | 历史对话 | 展示历史任务标题、摘要和时间。 |
| 设置 | 个人入口 | 承载个人中心、记忆中心、管理后台入口。 |

## 4. 功能需求设计说明

### 4.1 新建任务

#### 4.1.1 通用智能体

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| Agent/Plan 模式切换 |  | 用户可在新建任务输入框中切换 Agent 和 Plan。Agent 面向目标驱动的自动执行，Plan 面向计划生成、确认和分步执行。Agent/Plan 之间切换不新建任务。 |
| 技能调用 |  | 用户可从已安装技能中选择技能。选择后以 `/技能名` 插入输入框当前光标位置，支持插入在文本开头、中间或末尾。发送时系统识别技能并写入任务元信息。 |
| 模型选择 |  | 用户可选择当前任务使用的模型。对话中途切换模型时，提示“在对话中途切换模型会降低性能表现”。专家任务中模型选择隐藏或不可操作。 |
| 附件上传 |  | 普通任务支持附件上传，用于补充任务上下文。知识问答模式下附件上传隐藏。附件会影响上下文估算。 |

#### 4.1.2 定制智能体：知识问答、深度写作、PPT 创作

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 知识问答 |  | 用户选择知识问答后，任务按问答场景执行，输入框 placeholder 和默认提示词切换为知识查询表达。选择后隐藏技能选择和附件上传，可启用联网搜索和知识库搜索，用于补充实时信息和企业内部知识依据。 |
| 深度写作 |  | 用户选择深度写作后，任务按结构化写作场景执行，适合长文、报告、方案等内容生成。选择后隐藏技能选择，可启用联网搜索和知识库搜索，用于获取公开资料、行业信息、内部制度、历史方案等写作参考。 |
| PPT 创作 |  | 用户选择 PPT 创作后，展示 PPT 模板选择。选择模板后预填对应提示词，并在回答元信息中展示快捷类型和模板信息。可启用联网搜索和知识库搜索，用于补充汇报素材、数据依据、内部资料和案例内容。 |
| 搜索能力 |  | 定制智能体任务支持联网搜索和知识库搜索两个检索入口。联网搜索用于获取外部公开信息、实时信息和通用资料；知识库搜索用于检索企业内部文档、制度、方案、产品资料和历史沉淀。用户可按任务需要开启或关闭，系统在回答中应尽量标注信息来源类型。 |
| 定制智能体展示 |  | 知识问答、深度写作、PPT 创作选择后，占位展示在原模式/专家选择位置，用于明确当前任务类型。 |

#### 4.1.3 专家选择

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 召唤专家入口 |  | 用户在模式下拉菜单中点击“召唤专家”后，右侧展示专家选择弹窗。 |
| 专家搜索 |  | 专家弹窗顶部提供搜索框，支持按专家名称搜索。默认展示前 5 个结果，并支持滚动加载更多。 |
| 选择专家 |  | 点击专家后，将专家带入新建任务。专家任务中模型和技能不可选，回答只展示专家元信息。 |
| 对话中切换专家 |  | 当普通任务已有对话时，切换为专家需要重新创建新的任务，避免普通任务上下文与专家任务上下文混用。 |
| 专家模块跳转 |  | 用户在专家模块点击某个专家后，跳转到新建任务，并带入该专家和默认提示词。 |

#### 4.1.4 消息队列

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 队列触发 |  | 消息队列由会话执行状态触发：当上一轮会话尚未结束，包括助手仍在生成、工具仍在执行、文件仍在生成或队列消息正在处理时，用户新发送的消息先进入消息队列；当当前无进行中会话时，新消息直接进入正常提问回答流程。 |
| 队列位置 |  | 消息队列展示在对话框上方，紧贴输入区域，用于表达“待发送/待处理”的消息顺序。 |
| 队列编辑 |  | 队列消息支持编辑，编辑后保留当前位置。空消息不允许保存。 |
| 队列排序 |  | 队列消息支持上移、下移，用于调整后续发送顺序。第一条不可上移，最后一条不可下移。 |
| 队列删除 |  | 队列消息支持删除或关闭排队。删除后不再进入正常对话流程。 |
| 自动处理 |  | 队列消息按当前排序依次处理，间隔 10 秒自动进入正常提问回答流程。进入流程后先展示为用户提问，再触发助手回答。 |
| 手动引导执行 |  | 手动引导能力仅在通用智能体（AI Agent）任务中提供。用户手动将队列消息引导进入当前问答时，视为执行该条队列消息。引导中临时追加的内容与该条消息共用当前问答的配置执行，而不是入队时旧配置。定制智能体和专家任务的消息队列中不提供该入口。 |
| 执行配置快照 |  | 消息进入队列后会持续跟随当前任务最新配置变化，直到真正开始执行时才冻结当时的配置。队列消息未执行前，用户切换模型或其他配置，应自动影响尚未执行的消息。 |
| 队列隔离 |  | 消息队列归属于具体任务会话。新建任务会创建新的空队列，不影响原任务中尚未处理的队列消息；用户回到原任务时，应恢复原任务队列状态。 |

#### 4.1.5 上下文进度

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 上下文入口 |  | 开始对话后，在输入框工具栏展示上下文入口；未开始对话时隐藏。入口以百分比展示当前上下文使用量。 |
| 上下文浮层 |  | 点击入口后展示浮层，仅展示已使用比例、已使用量/总窗口、剩余可用。 |
| 上下文估算 |  | 系统根据当前模式、专家、技能、快捷任务、PPT 模板、输入内容、附件和历史消息估算上下文使用量。 |
| 上下文压缩提示 |  | 当上下文超过压缩阈值时，系统在对话过程中输出“正在压缩上下文...”提示；压缩完成后输出“上下文压缩完成”提示，再继续回答。 |
| 上下文重置 |  | Agent/Plan/Ask 普通任务共享上下文；切换专家新建任务后，上下文重新计算。 |

#### 4.1.6 Prompt 增强

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| Prompt 增强入口 |  | 输入框工具栏提供 Prompt 增强按钮，用于将简单输入优化为更完整的任务描述。 |
| 增强内容 |  | 增强后应补充任务目标、背景上下文、约束条件、交付物、时间要求和执行步骤等信息。 |
| 保留用户意图 |  | Prompt 增强不得改变用户原始目标，仅对表达、结构和执行要点进行补全。 |
| 与技能/专家协同 |  | 当输入中包含技能或已选择专家时，增强结果需保留对应技能和专家信息。 |

### 4.2 专家

#### 4.2.1 专家

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 专家 |  | 展示专家列表，包含专家名称、分类、描述和入口操作。支持点击专家进入新建任务。 |
| 专家团 |  | 展示专家团列表，用于多智能体协同场景。专家团可作为复杂任务的协作能力入口。 |
| 专家搜索 |  | 支持按专家名称和描述搜索专家或专家团。搜索结果需保持当前分类和 tab 上下文。 |
| 跳转新建任务 |  | 点击专家后跳转新建任务，并带入所选专家。新建任务页展示专家状态，模型和技能不可选。 |

#### 4.2.2 技能

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 技能市场 |  | 展示可安装技能，支持分类、搜索和安装。已安装技能在市场中展示已安装状态。 |
| 已安装 |  | 展示当前用户可调用技能。新建任务中的技能选择仅来源于已安装技能。 |
| 安装技能 |  | 用户在技能市场点击安装后，技能进入已安装列表，并进入新建任务可选范围。 |
| 上传技能 |  | 技能页的“添加技能”支持上传技能，打开上传弹窗。支持拖拽文件或点击上传，并校验 `SKILL.md` 或 `.md` 文件结构。 |
| 创建技能 |  | 技能页的“添加技能”支持创建技能，点击后跳转新建任务，并预填 `/skill-creator 请帮我创建一个可以实现「……」的skill`。 |

### 4.3 文件库

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 文件分组 |  | 文件库只展示有文件输出的会话，并按任务会话分组展示生成文件。 |
| 搜索 |  | 支持搜索任务名称或文件名称。命中任务名称时展示该任务下全部文件；命中文件名称时仅展示命中文件。 |
| 滚动加载 |  | 文件列表区域独立滚动。后续真实接口建议首次加载 20 个任务分组，滚动到底部后使用 cursor 加载下一批。 |
| 预览 |  | 文件操作菜单支持预览文件，预览弹窗展示文件名、类型、大小、生成时间、来源任务和可预览内容。 |
| 定位 |  | 文件操作菜单支持定位到任务。点击后跳转新建任务，恢复对应会话上下文并高亮文件生成消息。 |
| 下载 |  | 文件操作菜单支持下载文件。不可下载或下载失败时展示错误提示。 |

### 4.4 自动化

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 自动化列表 |  | 展示自动化任务名称、执行频率、生效区间和运行状态。 |
| 创建自动化 |  | 支持配置任务名称、任务提示词、执行频率、生效开始时间和结束时间。 |
| 执行历史 |  | 展示自动化执行记录，包括执行摘要、执行时间和执行状态。 |

### 4.5 最近任务

| 需求 | 原型 | 说明 |
| --- | --- | --- |
| 最近任务入口 |  | 左侧导航展示近期任务入口，帮助用户快速回到最近工作。 |
| 历史对话 |  | 历史页展示任务标题、摘要和时间。 |
| 回溯任务 |  | 用户可从历史或文件库定位回原始任务对话，继续查看上下文和结果。 |

## 5. 用户角色

- 普通用户：通过新建任务完成问答、写作、PPT、文件处理等日常工作。
- Agent 用户：使用 Agent 模式提交目标，由系统自动执行并输出结果。
- Plan 用户：使用 Plan 模式先查看计划，再确认分步执行。
- 专家使用者：选择指定专家完成专业任务。
- 技能使用者：安装、上传或创建技能，并在任务中调用已安装技能。
- 自动化使用者：配置周期性任务，查看执行结果。
- 管理员：通过管理后台维护用户、专家、技能、模型和系统配置。

## 6. 信息架构

左侧导航提供统一入口：

- 新建任务
- 专家
- 项目（二期，占位，不纳入本期）
- 自动化
- 文件库
- 最近任务
- 设置入口

设置入口承载：

- 个人中心
- 记忆中心
- 管理后台

导航规则：

- 当前页面高亮。
- 最近任务展示近期任务标题，点击后可回到对应历史对话。
- “项目（二期）”仅作为未来模块占位，本期不要求完整交互。

## 7. 功能需求

### 7.1 新建任务

新建任务是工作台核心入口，支持用户通过自然语言发起 AI 工作流。

核心能力：

- 支持 Agent、Plan、Ask、专家任务等任务模式。
- 支持快捷任务：知识问答、深度写作、PPT 创作。
- 支持定制智能体中的联网搜索和知识库搜索。
- 支持模型选择。
- 支持已安装技能选择。
- 支持附件上传。
- 支持 Prompt 增强。
- 支持上下文使用进度展示。
- 支持消息队列。
- 支持对话结果展示元信息。

模式说明：

- Agent：全自动执行者，目标驱动，全自动闭环。
- Plan：可控的规划师，计划-审批-分步执行。
- Ask：轻量问答模式，适合快速提问。
- 专家：由指定专家处理当前任务。

模式切换规则：

- Agent 和 Plan 之间切换不新建任务。
- Ask、Agent、Plan 可共享普通任务上下文。
- 对话过程中从 Agent / Plan / Ask 切换为专家时，需要重新创建新的任务。
- 已选择专家后，模型和技能不可选。
- 专家任务回答只展示专家元信息，不展示模式、模型、技能。

快捷任务规则：

- 知识问答、深度写作、PPT 创作被选择后，展示在原专家/模式选择位置。
- 选择快捷任务后，技能选择隐藏。
- 选择知识问答后，附件上传按钮隐藏。
- PPT 创作支持模板选择，模板选择后预填对应提示词。
- 知识问答、深度写作、PPT 创作均可支持联网搜索和知识库搜索。
- 联网搜索用于获取外部公开信息、实时信息、行业资料、通用解释和案例素材。
- 知识库搜索用于获取企业内部文档、制度规范、产品资料、历史方案、项目沉淀和业务口径。
- 用户可在定制智能体任务中按需开启联网搜索、知识库搜索，也可同时开启两类检索。
- 系统生成回答、文章或 PPT 内容时，应结合用户问题判断是否需要检索；检索结果应作为依据或素材补充，不应覆盖用户原始任务目标。
- 当回答使用联网搜索或知识库搜索结果时，结果中应尽量展示来源类型，例如“来源：联网搜索”“来源：知识库”，便于用户判断信息依据。
- 未开启搜索能力时，定制智能体仅基于当前输入、上下文和系统内置能力生成结果。

技能调用规则：

- 新建任务中的技能选择范围仅限已安装技能。
- 选择技能后，以 `/技能名` 的形式插入到输入框当前光标位置。
- 技能可插入在文本开头、中间或末尾。
- 支持输入 `/` 触发技能联想。
- 单次输入最多支持 10 个技能。
- 发送消息时，系统从输入文本中识别技能并写入元信息。

模型选择规则：

- 默认展示当前选中模型。
- 对话中途切换模型时，提示：“在对话中途切换模型会降低性能表现”。
- 专家任务中模型选择隐藏或不可操作。
- 对于已进入消息队列、尚未执行的消息，应使用当前最新配置执行，不受“入队时旧配置”限制。
- 用户切换模型后，未执行的队列消息和新发送消息都使用新的模型。
- 用户手动将排队消息引导进入当前问答时，该消息使用当前问答的模型执行；该能力仅适用于通用智能体（AI Agent）。
- 如果用户希望某条排队消息沿用旧配置，需要在配置变更前执行，或显式锁定该消息的执行配置。

消息队列规则：

- 消息队列不按发送次数触发，而按当前会话执行状态触发。
- 当前无进行中会话时，用户发送消息直接进入正常“提问 -> 回答”流程。
- 上一轮会话未结束时，包括助手仍在生成、工具仍在执行、文件仍在生成或队列消息正在处理，用户新发送的消息先进入队列。
- 队列位于对话框上方，紧贴对话框展示。
- 队列消息支持编辑、删除、上移、下移。
- 队列消息间隔 10 秒自动进入正常提问回答流程。
- 队列中的消息按当前排序依次处理。
- 队列消息在执行前持续跟随当前任务配置变化，只有开始执行时才冻结执行配置。
- 队列消息执行时默认使用执行瞬间页面上的最新配置，而不是入队时配置。
- 仅通用智能体（AI Agent）任务支持手动引导队列消息进入当前问答；定制智能体和专家任务的消息队列不提供该功能。
- 通用智能体任务中，队列消息被手动引导进入当前问答时，也属于执行该消息，执行配置以当前问答的配置为准。

消息队列需求分析：

- 队列的核心价值是承接用户在上一轮会话尚未完成时追加的新需求，避免新消息打断当前回答、工具调用或文件生成。
- 以“第几次发送”作为触发条件会导致误判：如果前两轮都已完成，第三条消息仍应直接发送；如果第一轮回答尚未结束，第二条消息也应进入队列。
- 因此队列触发应以任务执行状态为准，而不是以消息次数为准。
- 系统需要维护当前会话状态，至少区分空闲、回答中、工具执行中、文件生成中、队列处理中、失败待重试等状态。
- 当状态为空闲时，队列中的第一条消息可进入正常流程；当状态非空闲时，新消息保持排队。
- 消息队列应按任务会话隔离。新建任务代表创建新的任务上下文和新的队列容器，不应清空、移动或消费原任务队列。
- 队列消息的执行结果应可解释。若用户在消息入队后切换了模型，队列中已存在的消息仍使用入队时模型，避免同一条待处理消息的执行环境被隐式改变。

消息队列能力：

- 队列用于承接用户在任务执行过程中连续追加的新需求，避免当前回答被打断。
- 队列展示在对话框上方，保持与输入框紧邻，便于用户理解“待发送”的消息顺序。
- 队列中的每条消息需展示核心文本，长文本可截断但需支持编辑查看完整内容。
- 队列支持编辑消息内容，编辑后保留当前队列位置。
- 队列支持上移、下移，用于调整后续发送顺序。
- 队列支持删除，删除后不再进入正常对话流程。
- 队列支持关闭排队，关闭后该消息从队列中移除。
- 队列处理时需锁定当前正在进入对话流程的消息，避免重复触发。
- 队列消息进入正常流程后，先作为用户提问展示，再触发助手回答。
- 队列消息应展示或可展开查看当前执行配置，例如模型、模式、技能、搜索开关等，便于用户确认该消息将以什么配置执行。
- 通用智能体任务的队列可提供“引导”入口；定制智能体和专家任务的队列仅支持排队展示、编辑、排序、删除和自动处理，不提供“引导”入口。
- 若用户在通用智能体任务中手动引导前修改了模型或其他执行项，界面应明确提示：本次引导将使用当前问答的配置，包括当前模型、模式、技能和搜索开关等。
- 队列为空时，队列区域隐藏，不占用对话空间。
- 当前对话无进行中回答时，队列中的第一条消息可立即进入正常流程。
- 当前对话正在回答时，新消息保持排队状态，等待间隔时间或当前处理完成后进入。
- 用户新建任务后，原任务队列继续保留在原任务会话中。用户从最近任务、历史或文件库定位回原任务时，若原任务仍有排队消息，应继续展示并按原任务状态处理。

消息队列异常处理：

- 编辑空消息时，不允许保存，提示用户补充内容。
- 删除队列消息后，若队列为空，隐藏队列区域。
- 上移第一条消息或下移最后一条消息时，对应按钮置灰或不可操作。
- 用户新建任务或切换专家创建新任务时，仅新任务使用新的空队列；原任务队列不清空、不迁移、不自动进入新任务。
- 网络异常或回答失败时，当前队列消息可保留为待重试状态。

上下文能力：

- 新建任务需要展示当前对话的上下文使用情况，帮助用户了解任务容量。
- 上下文入口位于对话框工具栏中，开始对话后展示，未开始对话时隐藏。
- 上下文入口以百分比展示当前使用量，例如 `25.1%`。
- 点击上下文入口后展示上下文浮层。
- 浮层展示：
  - 已使用上下文比例。
  - 已使用量 / 总上下文窗口。
  - 剩余可用上下文。
- 上下文统计需根据当前模式、已选专家、已选技能、快捷任务、PPT 模板、输入内容和历史消息估算。
- 上下文浮层支持点击外部关闭。

上下文处理规则：

- Agent / Plan / Ask 普通任务共享同一普通任务上下文。
- 专家任务与普通任务上下文隔离；对话中切换专家会新建任务并重置上下文。
- 快捷任务切换后，需要基于快捷类型重新计算上下文估算。
- 技能插入、专家选择、附件上传和长文本输入都会增加上下文估算。
- 文件库定位回任务时，应恢复对应任务上下文并高亮目标消息。
- 当上下文接近阈值时，系统应自动压缩较早历史消息，并保留任务目标、关键约束、已选能力和重要结论。
- 当触发上下文压缩时，系统应在对话流中先输出“正在压缩上下文...”状态提示；压缩完成后输出“上下文压缩完成”提示，再继续回答。
- 压缩后继续对话时，回答应基于压缩后的摘要和最新消息生成。

对话展示规则：

- 开始对话后，对话框固定在页面底部。
- 对话内容从上往下输出。
- 对话较多时支持垂直滚动。
- 普通任务回答可展示模式、模型、技能等元信息。
- 快捷任务回答可展示快捷类型、模型、PPT 模板、联网搜索、知识库搜索等元信息。
- 通用智能体问答回答下方展示会话消耗信息，包含本次回答使用的模型、耗时、Token 消耗。
- 会话消耗信息展示在回答操作按钮同一行，位于重试、点赞、点踩、复制按钮之后。
- 会话消耗用于帮助用户理解本轮回答的执行成本与响应效率，不作为可编辑配置项。
- 专家任务回答仅展示专家信息。
- 专家任务不展示模型、耗时、Token 消耗等通用问答会话消耗信息。
- 上下文压缩提示属于系统状态提示，展示在对话流中，但不计入用户提问或助手正式回答内容。

### 7.2 专家模块

专家模块统一承载专家、专家团和技能能力。

一级切换：

- 专家
- 技能

专家能力：

- 展示专家列表。
- 支持专家分类筛选。
- 支持按专家名称和描述搜索。
- 支持专家团展示，用于多智能体协同场景。
- 点击专家后跳转新建任务，并带入所选专家。
- 从专家模块进入新建任务时，输入框可预填专家默认提示词。

专家选择规则：

- 新建任务中点击“召唤专家”后，右侧展示专家弹窗。
- 专家弹窗支持按名称搜索。
- 默认展示搜索结果前 5 个。
- 支持滚动加载更多专家。
- 点击“在 专家 中查找更多”跳转专家模块。
- 对话过程中选择专家时，重新创建专家任务。

### 7.3 技能模块

技能模块位于专家模块下，区分技能市场和已安装。

技能市场：

- 展示可安装技能。
- 支持技能分类。
- 支持按名称搜索。
- 支持安装技能。
- 已安装技能在市场中展示“已安装”状态。

已安装：

- 展示当前用户可调用技能。
- 支持搜索已安装技能。
- 点击“使用”后进入新建任务，并将技能带入输入框。
- 新建任务的技能下拉仅来源于已安装技能。

添加技能：

- 技能页展示“添加技能”按钮。
- 鼠标移入后展示两个操作：
  - 上传技能
  - 创建技能

上传技能：

- 点击“上传技能”打开上传弹窗。
- 弹窗标题为“上传技能”。
- 支持拖拽文件或点击上传。
- 支持“非高风险自动安装”选项。
- 文件夹或 .zip 需要包含 `SKILL.md` 文件。
- `.md` 文件需包含 YAML 格式的技能名称和描述。

创建技能：

- 点击“创建技能”跳转新建任务。
- 输入框预填：
  `/skill-creator 请帮我创建一个可以实现「……」的skill`
- 用户补充目标后发送，由系统引导生成技能定义。

### 7.4 自动化

自动化模块用于配置周期性任务和查看执行结果。

自动化任务列表：

- 展示自动化任务名称。
- 展示执行频率。
- 展示生效区间。
- 展示运行状态。
- 支持运行中、暂停等状态展示。

执行历史：

- 展示自动化执行记录。
- 展示执行摘要。
- 展示执行时间。
- 展示执行状态。

创建自动化：

- 支持填写任务名称。
- 支持填写任务提示词。
- 支持选择执行频率。
- 支持配置生效开始时间和结束时间。
- 支持一句话创建自动化：点击后跳转新建任务，并预填自动化创建提示词。

### 7.5 文件库

文件库统一管理任务生成文件和对话产物。当前页面只展示有文件输出的会话。

核心能力：

- 展示有文件输出的任务会话。
- 按任务分组展示生成文件。
- 支持搜索任务名称或文件名称。
- 支持预览文件。
- 支持定位到任务。
- 支持下载文件。
- 下载失败时展示错误反馈。

搜索规则：

- 搜索框支持输入任务名称或文件名称。
- 命中任务名称时，展示该任务下全部文件。
- 命中文件名称时，仅展示命中的文件。
- 无匹配结果时展示空状态：“没有找到匹配的任务或文件”。
- 搜索时不再提供“全部 / 最近 7 天 / 仅有文件的对话”等筛选切换。

文件操作：

- 文件行右侧展示三点操作按钮。
- 点击后展示菜单：
  - 预览文件
  - 定位到任务
  - 下载文件
- 预览文件打开弹窗，展示文件名、类型、大小、生成时间、来源任务和可预览内容。
- 定位到任务跳转新建任务页，恢复对应会话上下文，并高亮文件生成消息。
- 下载文件触发浏览器下载；不可下载或失败时展示错误提示。

滚动与加载：

- 文件库标题和搜索区固定在卡片顶部。
- 文件列表区域独立垂直滚动。
- 后续真实接口建议使用滚动加载，而非传统分页。
- 首次加载建议返回 20 个任务分组。
- 滚动到底部时加载下一批 20 个任务分组。
- 使用 cursor 分页，cursor 可由 `lastGeneratedAt + lastConversationId` 组成。
- 单个任务文件较多时，任务内展开更多文件可单独按文件 cursor 加载。

文件类型示例：

- PDF
- DOCX
- XLSX
- MD
- CSV
- JSON

### 7.6 历史与最近任务

历史与最近任务用于帮助用户回溯过去的工作。

能力包括：

- 左侧展示近期任务入口。
- 历史页展示历史对话。
- 展示任务标题、摘要和时间。
- 支持从文件库定位回原始任务对话。

### 7.7 设置与个人入口

用户设置入口统一承载：

- 个人中心
- 记忆中心
- 管理后台

设置入口要求：

- 入口隐藏在用户头像/账号菜单下。
- 菜单展示账号基础信息。
- 点击对应入口跳转对应页面或模块。

## 8. 关键流程

### 8.1 通用 Agent 任务流程

1. 用户进入新建任务。
2. 默认进入 Agent 模式。
3. 用户输入任务目标。
4. 系统理解目标、拆解任务并执行。
5. 输出回答或生成文件。
6. 任务结果沉淀到历史和文件库。

### 8.2 Plan 模式任务流程

1. 用户切换到 Plan。
2. 用户输入任务目标。
3. 系统输出执行计划。
4. 用户确认、修改或继续补充。
5. 系统按计划分步执行。
6. 输出最终结果。

### 8.3 消息队列处理流程

1. 用户开始普通任务对话。
2. 用户发送消息后，系统判断当前是否存在未结束会话。
3. 若当前为空闲状态，消息直接进入提问回答流程。
4. 若上一轮会话未结束，消息进入消息队列。
5. 用户可编辑、删除或调整队列顺序。
6. 上一轮会话结束后，系统按队列顺序处理第一条待处理消息。
7. 被处理消息从队列中移除，并作为用户提问追加到对话中。
8. 系统生成回答。
9. 若队列仍有消息，继续按当前会话状态、间隔和顺序处理下一条。

### 8.4 上下文查看流程

1. 用户开始对话后，上下文入口展示当前使用比例。
2. 用户点击上下文入口。
3. 系统展示上下文浮层。
4. 用户查看已使用比例、已使用量 / 总窗口、剩余可用。
5. 当上下文超过压缩阈值并进入执行流程时，系统在对话中输出“正在压缩上下文...”提示。
6. 压缩完成后，系统输出“上下文压缩完成”提示。
7. 系统继续输出后续回答。

### 8.5 专家任务流程

1. 用户在新建任务或专家模块选择专家。
2. 系统创建专家任务。
3. 新建任务页展示所选专家。
4. 模型和技能选择隐藏或不可操作。
5. 用户输入任务目标。
6. 专家输出专业回答或结果文件。

### 8.6 技能安装与调用流程

1. 用户进入专家模块。
2. 切换到技能。
3. 在技能市场选择技能并安装。
4. 技能进入已安装列表。
5. 用户在新建任务中通过技能选择或 `/` 调用已安装技能。
6. 系统在发送时识别技能并写入任务元信息。

### 8.7 上传技能流程

1. 用户进入专家模块的技能页。
2. 鼠标移入添加技能。
3. 点击上传技能。
4. 系统打开上传技能弹窗。
5. 用户选择文件或拖拽文件。
6. 系统校验文件格式和风险等级。
7. 符合要求后完成上传或安装。

### 8.8 创建技能流程

1. 用户进入专家模块的技能页。
2. 鼠标移入添加技能。
3. 点击创建技能。
4. 系统跳转新建任务。
5. 输入框预填 skill-creator 创建提示。
6. 用户补充技能目标并发送。
7. 系统引导生成技能定义。

### 8.9 文件定位流程

1. 用户进入文件库。
2. 搜索或浏览到目标文件。
3. 点击文件操作菜单。
4. 点击“定位到任务”。
5. 系统跳转新建任务页。
6. 系统恢复对应会话并高亮文件生成消息。

### 8.10 自动化创建流程

1. 用户进入自动化模块。
2. 点击创建自动化。
3. 填写任务名称、提示词、频率和生效时间。
4. 保存后进入自动化任务列表。
5. 系统按规则执行，并在执行历史中展示结果。

## 9. 信息与状态规则

### 9.1 任务元信息

- Agent / Plan / Ask 普通任务：可展示模式、模型、技能等信息。
- 快捷任务：可展示快捷类型、模型、PPT 模板等信息。
- 专家任务：只展示专家信息。
- 通用智能体回答下方展示会话消耗信息，包括模型、耗时、Token 消耗。
- 会话消耗展示属于回答结果信息，不单独占用配置区域；展示位置在回答操作按钮后方。
- Token 消耗默认展示总消耗，后续如需要可扩展为提示词、回复、缓存命中等拆分明细。
- 未选择技能时不展示技能信息。
- 选择专家后，不展示模式、模型、技能信息。
- 选择专家后，不展示模型、耗时、Token 消耗等会话消耗信息。
- 队列消息需要保存当前执行配置的可变引用。可变项包括模式、模型、专家、技能、定制智能体类型、PPT 模板、联网搜索、知识库搜索、附件引用和上下文引用。
- 队列消息在真正开始执行前，应始终跟随当前任务的最新配置变化；进入执行瞬间后，该次执行使用当时的配置。
- 仅通用智能体（AI Agent）任务的队列消息支持手动引导进入当前问答，并使用当前问答的配置。
- 若队列消息被编辑，默认仅修改文本内容；配置变更会继续跟随当前任务最新设置。

### 9.2 技能状态

- 技能市场：展示可安装技能。
- 已安装：展示用户可调用技能。
- 已安装技能可在新建任务中选择。
- 未安装技能不可在新建任务中选择。
- 安装技能后，技能进入已安装列表和新建任务可选范围。

### 9.3 对话队列状态

- 未排队：当前消息直接进入正常对话流程。
- 排队中：消息等待进入正常对话流程。
- 编辑中：用户正在修改队列消息。
- 待处理：消息已排队，等待计时或前序消息完成。
- 处理中：消息已从队列移除并进入正常提问回答流程。
- 已移除：消息被删除、关闭排队或处理完成。
- 失败待重试：消息进入流程失败或回答失败，允许用户重试或删除。

队列状态规则：

- 当前会话为空闲状态时，新消息直接进入正常对话。
- 当前会话未结束时，新消息进入消息队列。
- 队列消息按顺序自动处理。
- 用户可编辑队列消息。
- 用户可调整队列顺序。
- 队列消息被处理后从队列移除。
- 同一时刻仅允许一条队列消息进入处理状态。
- 队列顺序变化后，后续自动处理按最新顺序执行。
- 模型切换会影响所有尚未执行的队列消息；已开始执行的消息不受影响。
- 仅通用智能体（AI Agent）任务支持手动引导队列消息进入当前问答；定制智能体和专家任务不支持该能力。

### 9.4 上下文状态

- 未开始：未产生对话，上下文入口隐藏。
- 正常：上下文使用量处于安全区间。
- 超限风险：输入或附件可能导致上下文超限，需要提示用户拆分任务。

上下文状态规则：

- 开始对话后展示上下文使用入口。
- 每次发送消息、选择技能、选择专家、选择快捷任务、上传附件或插入长文本后，需要重新估算上下文。
- 普通任务模式切换不清空上下文。
- 切换专家新建任务后，新任务上下文重新计算；原任务上下文和队列状态保留在原任务中。
- 上下文浮层关闭后，不影响上下文统计和任务执行。

### 9.5 文件状态

- 可预览：支持打开预览弹窗。
- 可下载：支持浏览器下载。
- 不可下载：展示失败提示。
- 可定位：支持跳转到原始任务。
- 已删除或不存在：展示错误提示。

### 9.6 自动化状态

- 运行中。
- 暂停。
- 待执行。
- 已完成。
- 执行失败。

## 10. 视觉与交互原则

- 整体遵循 Apple 风格：简洁、精致、轻量、低噪音。
- 页面以任务执行为核心，避免营销式布局。
- 信息层级通过字号、色阶、间距和轻量分割体现。
- 工具栏保持紧凑，主操作明确。
- 浮层、菜单、弹窗不遮挡核心内容。
- 菜单与弹窗尺寸适中，字号克制。
- 对话框、队列和任务结果之间保持清晰层级。
- 卡片用于专家、技能、文件任务分组等重复对象。
- 长内容必须支持滚动，且滚动区域应清晰。

## 11. 非功能要求

- 页面需支持长内容垂直滚动。
- 文件库列表需支持独立滚动，为后续滚动加载预留。
- 关键交互需避免布局跳动。
- 状态切换需保持当前上下文不丢失，除非规则要求新建任务。
- 弹窗支持点击遮罩关闭和 Esc 关闭。
- 输入框、按钮、菜单需具备基本可访问性属性。
- 下载、定位、上传等操作需有成功或失败反馈。
- 原型阶段数据可使用前端模拟，后续需接入真实接口。

## 12. 验收标准

- 用户可通过统一工作台进入本期所有前台模块。
- 新建任务支持 Agent、Plan、Ask、专家、快捷任务、技能调用和消息队列。
- 当前会话为空闲时，新消息直接进入正常对话；上一轮会话未结束时，新消息进入消息队列。
- 消息队列支持编辑、删除、上移、下移和自动进入正常流程。
- 队列消息进入正常流程后，会作为用户提问展示并触发回答。
- 已排队但未执行的消息使用当前最新执行配置；入队后切换模型，会影响已有队列消息以及后续新发送或新入队消息。
- 仅通用智能体（AI Agent）任务支持手动引导队列消息进入当前问答；定制智能体和专家任务的消息队列不提供该能力。
- 队列为空时，队列区域自动隐藏。
- 新建任务开始对话后展示上下文使用入口。
- 上下文浮层可展示已使用比例、已使用量 / 总窗口、剩余可用。
- 上下文超过压缩阈值时，对话流中会输出“正在压缩上下文...”与“上下文压缩完成”提示。
- 切换专家新建任务后，新任务上下文和队列按新任务重新计算，原任务上下文和队列不受影响。
- 对话中选择专家会重新创建专家任务。
- 专家任务只展示专家元信息。
- 模型中途切换会展示性能提示。
- 专家模块可展示专家、专家团、技能市场和已安装技能。
- 技能市场支持安装技能。
- 已安装技能可在新建任务中调用。
- 技能模块支持添加技能、上传技能和创建技能。
- 自动化可创建任务并查看执行历史。
- 文件库只展示有文件输出的会话。
- 文件库支持任务名称和文件名称搜索。
- 文件库支持预览、定位到任务和下载文件。
- 文件库列表可垂直滚动。
- 项目模块不纳入本期验收范围。
- 整体 UI 风格统一、精致、简洁。

## 13. 二期规划

项目模块作为二期开发范围，后续可单独输出项目模块 PRD。

二期可包含：

- 项目列表。
- 项目详情。
- 项目任务。
- 项目成员。
- 项目邀请链接。
- 申请加入项目。
- 同意或拒绝申请。
- 项目级指令、文件、技能、专家和自动化配置。
- 项目上下文注入新建任务。

## 14. 后续接口与工程规划

- 接入真实用户、专家、技能、文件和自动化接口。
- 文件库使用 cursor 滚动加载。
- 支持技能安装权限校验和风险评估。
- 支持上传技能后的解析、校验、预览和发布。
- 支持 skill-creator 生成技能后自动进入待安装状态。
- 支持多模型策略和模型权限控制。
- 支持企业知识库统一检索。
